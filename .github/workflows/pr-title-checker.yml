name: "PR Title Checker"
on:
  pull_request:
    types:
      - opened
      - edited
      - synchronize
      - labeled
      - unlabeled

jobs:
  check_pr_title:
    name: "Check PR title"
    runs-on: ubuntu-latest
    steps:
      - name: Check commit type
        run: |
          VALID_COMMIT_TYPES="chore|ci|docs|feat|fix|refactor|test"
          REGEX="^((Merge[ a-z-]* branch.*)|(Revert*)|((${VALID_COMMIT_TYPES})(\(.*\))?!?: .*))"

          COMMIT_MESSAGE="${{ github.event.pull_request.title }}"

          echo "Commit Message: ${COMMIT_MESSAGE}"

          if ! [[ $COMMIT_MESSAGE =~ $REGEX ]]; then
            echo "::error title=Commit type::Incorrect type used. Valid types are ${VALID_COMMIT_TYPES}"
            exit 1
          else
            echo "::notice title=Commit type::All good"
          fi
